#include <bits/stdc++.h>
int n, m;
struct Node {
};
void add(int x, int y, int z)
{
}
int main()
{
    scanf("%d %d", &n, &m);
    for (int i = 1, u, v; i <= m; i++)
    {
        scanf("%d %d", &u, &v);
    }
    return 0;
}
